name: Forward PR to StegVerse AI Bridge [CONTAINED — NO CROSS-REPO AUTHORITY]

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  contained:
    runs-on: ubuntu-latest
    steps:
      - name: Block legacy bridge dispatch
        shell: bash
        run: |
          set -euo pipefail
          echo "::error::This bridge-dispatch path is contained because FREE-DOM's canonical handoff grants no cross-repository mutation/dispatch authority and the prior workflow used a generic non-TV/TVC credential."
          echo "Any future external review transport must be separately admitted, TV/TVC-governed, and evidence-only with no canonical record promotion authority."
          exit 1
